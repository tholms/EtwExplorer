
<UserControl x:Class="EtwExplorer.Views.TasksTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EtwExplorer.Views"
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             xmlns:z="http://zodiacon.wpf/2016"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Search:" VerticalAlignment="Center" />
            <TextBox Margin="4,0,0,0" Width="300" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                     metro:TextBoxHelper.Watermark="Type to search" metro:TextBoxHelper.ClearTextButton="True" z:FocusProperties.FocusOnLoad="True" />
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding TaskGroups}" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow" SelectedItem="{Binding SelectedTaskGroup, Mode=TwoWay}"
                  metro:ButtonHelper.PreserveTextCase="True" RowDetailsVisibilityMode="VisibleWhenSelected"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border BorderBrush="{DynamicResource BlackBrush}" BorderThickness="1" Margin="4" Padding="8" Background="#FFF9F9F9">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="8" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Template Details Side-by-Side with Resizable Splitters -->
                            <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                <ItemsControl ItemsSource="{Binding TemplateDetails}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" MinWidth="250" />
                                                    <ColumnDefinition Width="5" />
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" BorderBrush="{DynamicResource GrayBrush7}" BorderThickness="1" Margin="4">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="*" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock Text="{Binding TemplateName}" FontWeight="Bold"
                                                                  Padding="4" Background="{DynamicResource AccentColorBrush}" Foreground="White"/>
                                                        <DataGrid Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{Binding TemplateData}"
                                                                 IsReadOnly="True" HeadersVisibility="Column" BorderThickness="0"
                                                                 CanUserResizeColumns="True" CanUserSortColumns="False"
                                                                 MaxHeight="300" HorizontalScrollBarVisibility="Auto">
                                                            <DataGrid.Columns>
                                                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180" MinWidth="80" FontWeight="SemiBold" CanUserResize="True"/>
                                                                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="150" MinWidth="80" CanUserResize="True"/>
                                                            </DataGrid.Columns>
                                                        </DataGrid>
                                                    </Grid>
                                                </Border>
                                                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                                                             Background="{DynamicResource GrayBrush5}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <!-- Diff Section (only show if multiple templates) -->
                            <StackPanel Grid.Row="2" Orientation="Horizontal" Visibility="{Binding HasMultipleTemplates, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="Compare:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0"/>
                                <TextBlock Text="Template" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <ComboBox ItemsSource="{Binding DisplayTemplates}" SelectedIndex="{Binding SelectedTemplate1}"
                                         DisplayMemberPath="DisplayName" Width="150" Margin="0,0,8,0"/>
                                <TextBlock Text="vs" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0"/>
                                <TextBlock Text="Template" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <ComboBox ItemsSource="{Binding DisplayTemplates}" SelectedIndex="{Binding SelectedTemplate2}"
                                         DisplayMemberPath="DisplayName" Width="150" Margin="0,0,8,0"/>
                                <Button Content="Diff" Padding="12,4"
                                       Command="{Binding DataContext.DiffCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       CommandParameter="{Binding}"
                                       IsEnabled="{Binding CanDiff}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Task Name" Binding="{Binding TaskName}" Width="*" FontWeight="Bold" />
                <DataGridTextColumn Header="Templates" Binding="{Binding TemplateCount}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

<UserControl x:Class="EtwExplorer.Views.EventDiffTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Side -->
        <DockPanel Grid.Column="0">
            <TextBlock DockPanel.Dock="Top" Text="{Binding LeftVersionLabel}"
                      FontWeight="Bold" Margin="0,0,0,5" Background="#FFF0F0F0" Padding="5"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding LeftLines}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Background}" BorderThickness="0">
                                <TextBlock Text="{Binding Text}" FontFamily="Consolas" FontSize="11"
                                          Padding="2,0" TextWrapping="NoWrap"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#FFCCCCCC"/>

        <!-- Right Side -->
        <DockPanel Grid.Column="2">
            <TextBlock DockPanel.Dock="Top" Text="{Binding RightVersionLabel}"
                      FontWeight="Bold" Margin="0,0,0,5" Background="#FFF0F0F0" Padding="5"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding RightLines}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Background}" BorderThickness="0">
                                <TextBlock Text="{Binding Text}" FontFamily="Consolas" FontSize="11"
                                          Padding="2,0" TextWrapping="NoWrap"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>

<UserControl x:Class="EtwExplorer.Views.EventsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EtwExplorer.Views"
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             xmlns:z="http://zodiacon.wpf/2016"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Search:" VerticalAlignment="Center" />
            <TextBox Margin="4,0,0,0" Width="300" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"
                     metro:TextBoxHelper.Watermark="Type to search" metro:TextBoxHelper.ClearTextButton="True" z:FocusProperties.FocusOnLoad="True" />
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Events}" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  metro:ButtonHelper.PreserveTextCase="True"
                  RowDetailsVisibilityMode="VisibleWhenSelected"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Go to Task" Click="GoToTask_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border BorderBrush="{DynamicResource BlackBrush}" BorderThickness="1" Margin="4" Padding="8" Background="#FFF9F9F9">
                        <Border BorderBrush="{DynamicResource GrayBrush7}" BorderThickness="1" Margin="4" Width="350" HorizontalAlignment="Left">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Template, StringFormat='Template: {0}'}" FontWeight="Bold"
                                          Padding="4" Background="{DynamicResource AccentColorBrush}" Foreground="White"/>
                                <DataGrid Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{Binding TemplateData}"
                                         IsReadOnly="True" HeadersVisibility="Column" BorderThickness="0"
                                         CanUserResizeColumns="True" CanUserSortColumns="False"
                                         MaxHeight="300" HorizontalScrollBarVisibility="Auto">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180" MinWidth="80" FontWeight="SemiBold" CanUserResize="True"/>
                                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="150" MinWidth="80" CanUserResize="True"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Symbol}" Width="250" FontWeight="Bold" />
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="70" ElementStyle="{StaticResource DataGridCellRightJustifiedStyle}"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="80" ElementStyle="{StaticResource DataGridCellRightJustifiedStyle}"/>
                <DataGridTextColumn Header="Task" Binding="{Binding Task}" Width="*" MinWidth="150" />
                <DataGridTextColumn Header="Keyword" Binding="{Binding Keyword}" Width="200" />
                <DataGridTextColumn Header="Template" Binding="{Binding Template}" Width="150" />
                <DataGridTextColumn Header="Opcode" Binding="{Binding Opcode}" Width="120" />
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
